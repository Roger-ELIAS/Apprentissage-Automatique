import matplotlib.image as mpimg
import numpy as np
import os

import matplotlib.pyplot as plt
from PIL import Image



#Ce fichier c'est le zoo on fait des tests


#Récupérer les images sous forme de numpyArray
img = mpimg.imread("Data/Mer/aaaaa.jpeg")

imgplot = plt.imshow(img)

printImg = Image.fromarray(img, 'RGB')

print(img.shape)

#imread -> Matrice[x][y][z] avec x la position verticale du pixel sur l'image, y la position horizontale, et z = 0 -> valeur de R, z = 1 -> valeur de G ,z = 2 -> valeur de B

#Parcourir les fichiers

files = os.listdir("Data/Mer")

#for fileName in files :
#    print(fileName)

